enum RequestState {
  idle,
  loading,
  loginloaded,
  userdataloading,
  userdataloaded,
  error,
}

enum AllProductsRequestState {
  idle,
  loading,
  loaded,
  error,
}

enum BlogsRequestState {
  loading,
  loaded,
  error,
}
